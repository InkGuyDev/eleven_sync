name: eleven_sync
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  change_app_package_name: ^1.5.0
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  flutter_svg: ^2.2.0
  google_fonts: ^6.2.1
  path: ^1.9.1
  path_provider: ^2.1.5
  provider: ^6.1.5
  sensors_plus: ^6.1.1
  shared_preferences: ^2.5.3
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_launcher_icons:
  image_path: "assets/icons/final_logo.png"
  android: "ic_launcher"
  ios: false
  min_sdk_android: 21

flutter:
  uses-material-design: true
